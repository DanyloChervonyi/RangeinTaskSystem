services:
  postgres:
    image: postgres:16-alpine
    container_name: rangein-task-system-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rangein_task_system
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rangein_task_system"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
