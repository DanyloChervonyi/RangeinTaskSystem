apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      containers:
        - name: users-service
          image: rangein-task-system-users-service:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: rangein-config
            - secretRef:
                name: rangein-secret
